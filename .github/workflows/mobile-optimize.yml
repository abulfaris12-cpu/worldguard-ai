name: Deprecated mobile workflow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated - responsive layout is maintained directly in dashboard.html"